#include <stdio.h>
int main()
{
    double x,result;
    int N;
    x=1.3;
    N=5;
    result=pow(x,N);//this pow function use to get power any number

    printf("result=%.2f",result);

    return 0;
}
